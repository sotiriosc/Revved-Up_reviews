{{! searchResults.handlebars }}
{{! The layout file is used as a template and the contents of this file will be injected into the {{{body}}
{{! Make sure to include this file in the views folder. }}


    {{!-- <section class="container mx-auto px-6">
      <h2 class="text-center text-3xl my-6">Search Results</h2>

      {{#if carReviews.length}}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {{#each carReviews}}
            <div class="bg-white p-4 rounded-lg shadow-md">
              <h3 class="text-xl font-bold mb-2">{{this.title}}</h3>
              <p class="text-gray-700">{{this.carReview}}</p>
              <p class="text-gray-600 text-sm mt-4">Created by: {{this.creator.username}}</p>
              <a href="/carReview/{{this.id}}" class="text-blue-500 hover:text-blue-700 mt-2 inline-block">Read more</a>
            </div>
          {{/each}}
        </div>
      {{else}}
        <p class="text-center text-lg">No results found for your search query. Please try again with a different term.</p>
      {{/if}}
    </section>
 
 --}}


  <div class="container">
  <h2 class="searchText text-center p-6">Search Results:</h2>
  {{#if searchResult.length}}
    {{#each searchResult as |carReview|}}
      <div class="flex flex-col w-full max-[426px]:mt-5 max-[426px]:mb-1">
        <div class="flex flex-col items-center px-6 max-[426px]:px-4">
          <div class="px-4 w-[70%] max-[426px]:w-full bg-white mx-4 my-2 shadow-sm justify-between py-2 rounded-lg flex flex-col carReviewBox">
            <div class="font-semibold flex flex-row max-[426px]:flex-col max-[426px]:gap-2 gap-1 justify-between headerRow">
              <a class="carReviewTitle" href="/carReview/{{carReview.id}}">{{carReview.title}}</a>
              <p>Make: {{carReview.carmake}};&nbsp;&nbsp; Model: {{carReview.carmodel}}</p>
            </div>
            <div class="border border-dashed mt-2"></div>
            <div class="mt-2 carReviewDesc">
              <p class="carReviewDescMain">Review:<br>{{truncateContent carReview.carReview 20}}</p>
              <p class="carReviewCreate text-right italic">Created by {{carReview.creator.username}} on {{format_date carReview.date_reviewcreated}}</p>
            </div>
          </div>
        </div>
      </div>
    {{/each}}
  {{else}}
    <p>No results found.</p>
  {{/if}}
</div>

